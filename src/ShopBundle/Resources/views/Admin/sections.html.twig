{% extends '@Shop/base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-header">
                    <a href="{{ path('sectionAdd') }}" class="btn btn-primary">Создать <i class="fa fa-fw fa-plus-square"></i></a>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table id="example1" class="table table-bordered table-striped">
                        <thead>
                        <tr>
                            <th>ID</th>
                            <th>Название</th>
                            <th>Url</th>
                            <th>Изображение</th>
                            <th></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for section in sections%}
                            <tr>
                                <td>{{ section.id }}</td>
                                <td>{{ section.name }}</td>
                                <td>{{ section.code }}</td>
                                <td><img src="{{ section.picture }}"></td>
                                <td>
                                    <a href="{{ path('sectionEdit', {'id' : section.id} ) }}" class="btn btn-info">Редактировать <i class="fa fa-fw fa-edit"></i></a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>

                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
{% endblock %}